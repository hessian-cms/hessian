{
  "name": "hessian-cms",
  "version": "0.0.0",
  "repository": "https://github.com/hessian-cms/hessian.git",
  "author": "no4any",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "config/*",
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run --coverage",
    "test:coverage": "npm run --workspaces --if-present test:coverage",
    "build": "npm run --workspaces --if-present build"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.2",
    "vitest": "^4.1.2"
  }
}
